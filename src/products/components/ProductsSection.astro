---
import ProductButton from './ProductButton.astro';
import ProductImage from './ProductImage.astro';
import ProductPrice from './ProductPrice.astro';
import ProductSizes from './ProductSizes.astro';
const { data } = Astro.props;
---

<section
  class='grid grid-cols-[repeat(auto-fill,minmax(160px,1fr))] gap-2 px-2 pb-4'
>
  {
    data.map(({ data }: { data: any }) => {
      const { name, image, sizes, price, description } = data;
      return (
        <article class='h-fit flex flex-col border-2 border-black-medium'>
          <ProductImage image={image} />
          <div class='flex flex-col gap-1 leading-5 p-2 border-t-2 border-black-medium bg-orange-200/30'>
            <h2 class='font-semibold opacity-80 tracking-wide px-px'>{name}</h2>
            <ProductPrice price={price} />
            <p class='text-[12px] opacity-80 text-pretty leading-[14px] px-px'>
              {description}
            </p>
            <ProductSizes sizes={sizes} />
            <ProductButton name={name} />
          </div>
        </article>
      );
    })
  }
</section>
